
const Footer = ({primaryLabel}:any) => {
    return (
        <footer>
            <small className="primaryFontColor">{primaryLabel}</small>         
        </footer>
    )
}

export default Footer;